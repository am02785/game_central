%h1 Listing games

%table
  %thead
    %tr
      %th Title
      %th Description
      %th Image url
      %th Price
      %th Release date
      %th
      %th
      %th

  %tbody
    - @games.each do |game|
      %tr
        %td= game.title
        %td= game.description
        %td= game.image_url
        %td= game.price
        %td= game.release_date
        %td= link_to 'Show', game

%br

