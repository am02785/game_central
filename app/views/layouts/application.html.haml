!!!

%html
  %head
    %meta{'charset' => 'utf-8'}
    %meta{'http-equiv' => 'x-ua-compatible', 'content' => 'ie=edge'}

    / The title of the application.
    %title= t('app_title')

    / The description of the application.
    %meta{'name' => 'description', 'content' => t('app_description')}
    %meta{'name' => 'viewport', 'content' => 'width=device-width, initial-scale=1'}

    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = javascript_include_tag 'modernizr'
    = csrf_meta_tags

  %body

    / A containing element to make the site responsive to screen size.
    / https://www.w3schools.com/bootstrap/bootstrap_get_started.asp used to make the site responsive to screen size.
    %div{t('class') => t('container-fluid')}
      / The header of every page.
      = render t('header_file_path')
      = yield
      / The footer of every page.
      = render t('footer_file_path')
